name: Deploy to Render

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy with Render Hook
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Render deploy hook
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"